[project]
name = "mini-site-django"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django-colorfield>=0.11.0",
    "django>=5.1.2",
    "setuptools>=75.1.0",
]

[tool.uv]
dev-dependencies = [
    "djlint>=1.35.2",
]
